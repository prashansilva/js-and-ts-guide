// Q1
const nums = [3, 6, 9, 12, 15, 20];
// Write code to find the sum of only even numbers.
// Use a loop and condition.

// Q2
const numbers = [12, 45, 67, 23, 89, 5, 100, 34];
// Write code to find the max number of given numbers.
// Use a loop and condition

// Q3
const data = [1, "apple", 2, "banana", true, 42, "cherry"];
// Write code that counts how many items are numbers and how many are strings.

// Q4
const students = [
    { name: "Alice", grades: { math: 85, english: 70 } },
    { name: "Bob", grades: { math: 60, english: 90 } },
    { name: "Charlie", grades: { math: 95, english: 88 } }
];
// Print student names whose math score is >= 80.

// Q5
// Given a string:
const word = "JAVASCRIPT";

// Reverse it manually using a loop (don’t use built-in reverse).

// Q6
// Loop numbers from 1 to 20:
// - If divisible by 3 → print "Fizz"
// - If divisible by 5 → print "Buzz"
// - If divisible by both → print "FizzBuzz"
// - Otherwise print the number
